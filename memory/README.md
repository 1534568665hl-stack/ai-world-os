# Memory

This directory holds memory-related components and stored context.

Planned subfolders:

- `short_term/` - temporary, session-scoped context
- `long_term/` - persistent remembered facts and summaries
- `working/` - active context or in-progress notes
- `archives/` - older memory snapshots or historical records
