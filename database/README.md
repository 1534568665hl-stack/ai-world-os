# Database

This directory is reserved for persistence structure and data definitions.

Planned subfolders:

- `schemas/` - schema definitions and contracts
- `migrations/` - database migration scripts
- `seeds/` - seed data for development and testing
